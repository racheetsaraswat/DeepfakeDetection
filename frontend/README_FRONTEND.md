Frontend (Vite + React + TypeScript + Tailwind)
===============================================

Run
---
- `npm install`
- `npm run dev` (http://localhost:5173)

Environment
-----------
- Uses `VITE_API_BASE` when provided (defaults to http://localhost:8000).

Testing
-------
- `npm test`
















